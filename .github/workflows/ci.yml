name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      PYTHONHASHSEED: "0"
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e .[test]

      - name: Run tests (pass 1)
        run: |
          pytest -q --disable-warnings --maxfail=1 | tee out1.txt

      - name: Run tests (pass 2)
        run: |
          pytest -q --disable-warnings --maxfail=1 | tee out2.txt

      - name: Determinism check
        run: |
          diff -u out1.txt out2.txt      - run: pip install -e .
      - name: Run pytest first time
      pytest -q tests/test_irreversibility.py
        run: pytest -q > run1.txt
      pytest -q tests/test_irreversibility.py
      - name: Run pytest second time
      pytest -q tests/test_irreversibility.py
        run: pytest -q > run2.txt
      pytest -q tests/test_irreversibility.py
      - name: Verify bit-identical outputs
        run: |
          diff run1.txt run2.txt
          echo "✅ Tests are deterministic - outputs identical"
>>>>>>> 3d8db75 (Add core deterministic modules and documentation)
>>>>>>> e299c43 (Add core deterministic modules and documentation)
