[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "spectrum"
version = "0.0.0"
description = "Spectrum"
readme = "README.md"
requires-python = ">=3.8"