def find_irreversible(*args, **kwargs):
    """Irreversibility detector (MVP stub).

    Accepts arbitrary arguments and returns an empty set.
    Provided to maintain deterministic test imports.
    """
    return set()
